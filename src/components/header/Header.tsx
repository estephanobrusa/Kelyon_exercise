import { HeaderContainer, HeaderSubTitle, HeaderTitle } from './Header.style'

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Network</HeaderTitle>
      <HeaderSubTitle>network list</HeaderSubTitle>
    </HeaderContainer>
  )
}

export default Header
